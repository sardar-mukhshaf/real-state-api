package com.realestate.addresses.application;

import com.realestate.addresses.domain.PropertyAddress;
import com.realestate.shared.application.Store;

public interface PropertyAddressRepository extends Store<PropertyAddress> {}
