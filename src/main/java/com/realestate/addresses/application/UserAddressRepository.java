package com.realestate.addresses.application;

import com.realestate.addresses.domain.UserAddress;
import com.realestate.shared.application.Store;

public interface UserAddressRepository extends Store<UserAddress> {}
