package com.realestate.addresses.application;

import com.realestate.addresses.domain.Address;
import com.realestate.shared.application.Store;

public interface AddressRepository extends Store<Address> {}
